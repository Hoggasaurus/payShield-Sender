# payShield-Sender
payShield Command Sender — A Python GUI for sending and testing commands against Thales payShield HSMs over TCP, TLS, and UDP, with dual-target support, load testing, and session logging.
